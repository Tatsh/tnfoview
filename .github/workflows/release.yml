---
jobs:
  publish-release:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1'
        with:
          persist-credentials: false
      - env:
          GH_TOKEN: '${{ github.token }}'
          HEAD_SHA: '${{ github.event.workflow_run.head_sha }}'
          REPO: '${{ github.repository }}'
          TRIGGER_CONCLUSION: '${{ github.event.workflow_run.conclusion }}'
        id: 'guard'
        name: 'Check trigger conditions'
        run: |
          if [[ "$TRIGGER_CONCLUSION" != 'success' ]]; then
            echo "Triggering workflow concluded with '${TRIGGER_CONCLUSION}', nothing to do."
            echo 'proceed=false' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          tag=''
          while IFS=$'\t' read -r ref obj_type obj_sha; do
            [[ -z "$ref" ]] && continue
            if [[ "$obj_type" == 'commit' ]]; then
              target_sha="$obj_sha"
            else
              target_sha=$(gh api "repos/${REPO}/git/tags/${obj_sha}" \
                --jq '.object.sha' 2>/dev/null || echo '')
            fi
            if [[ "$target_sha" == "$HEAD_SHA" ]]; then
              tag="${ref#refs/tags/}"
              break
            fi
          done < <(gh api "repos/${REPO}/git/matching-refs/tags/v" \
            --jq '.[] | [.ref, .object.type, .object.sha] | @tsv')
          if [[ -z "$tag" ]]; then
            echo "No v* tag points at ${HEAD_SHA}, nothing to do."
            echo 'proceed=false' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "Found release tag ${tag} for SHA ${HEAD_SHA}."
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo 'proceed=true' >> "$GITHUB_OUTPUT"
      - env:
          GH_TOKEN: '${{ github.token }}'
          HEAD_SHA: '${{ github.event.workflow_run.head_sha }}'
          TAG: '${{ steps.guard.outputs.tag }}'
        id: 'check'
        if: "steps.guard.outputs.proceed == 'true'"
        name: 'Check all required workflows succeeded'
        run: |
          tag="$TAG"
          sha="$HEAD_SHA"
          required_workflows=('Build' 'Flatpak' 'Snap')
          optional_workflows=('Prettier' 'Spelling' 'Tests' 'clang-format' 'markdownlint')
          all_success=true
          process_workflow() {
            local workflow="$1" treat_missing="$2"
            local run status conclusion
            run=$(gh run list --workflow "$workflow" --commit "$sha" --json status,conclusion --jq '.[0]') || run=''
            if [[ -z "$run" || "$run" == 'null' ]]; then
              if [[ "$treat_missing" == 'pending' ]]; then
                echo "Required workflow '$workflow' has not registered a run yet."
                return 2
              fi
              echo "Optional workflow '$workflow' did not run for this commit; skipping."
              return 0
            fi
            status=$(echo "$run" | jq -r '.status') || status=''
            conclusion=$(echo "$run" | jq -r '.conclusion') || conclusion=''
            case "$conclusion" in
              failure|cancelled|timed_out|startup_failure|action_required)
                echo "::error::Workflow '$workflow' ${conclusion}."
                return 1
                ;;
              success|skipped|neutral)
                return 0
                ;;
              *)
                if [[ "$status" == 'completed' ]]; then
                  echo "::error::Workflow '$workflow' completed with unexpected conclusion '${conclusion}'."
                  return 1
                fi
                echo "Workflow '$workflow' still pending (status=${status})."
                return 2
                ;;
            esac
          }
          for workflow in "${required_workflows[@]}"; do
            rc=0
            process_workflow "$workflow" pending || rc=$?
            [[ $rc -eq 1 ]] && exit 1
            [[ $rc -eq 2 ]] && all_success=false
          done
          for workflow in "${optional_workflows[@]}"; do
            rc=0
            process_workflow "$workflow" skip || rc=$?
            [[ $rc -eq 1 ]] && exit 1
            [[ $rc -eq 2 ]] && all_success=false
          done
          if [[ "$all_success" != 'true' ]]; then
            echo 'Not all required workflows have completed yet.'
            exit 0
          fi
          echo "All required workflows succeeded for ${tag}."
          echo "ready=true" >> "$GITHUB_OUTPUT"
      - env:
          GH_TOKEN: '${{ github.token }}'
          TAG: '${{ steps.guard.outputs.tag }}'
        id: 'assets'
        if: "steps.check.outputs.ready == 'true'"
        name: 'Verify all built assets are attached'
        run: |
          mapfile -t asset_names < <(gh release view "$TAG" --json assets --jq '.assets[].name')
          if [[ ${#asset_names[@]} -eq 0 ]]; then
            echo "::error::Release ${TAG} has no assets attached."
            exit 1
          fi
          printf 'Attached asset: %s\n' "${asset_names[@]}"
          missing=false
          while IFS=':' read -r pattern minimum; do
            [[ -z "$pattern" ]] && continue
            count=0
            for name in "${asset_names[@]}"; do
              # shellcheck disable=SC2053
              if [[ "$name" == $pattern ]]; then
                count=$((count + 1))
              fi
            done
            if [[ "$count" -lt "$minimum" ]]; then
              echo "::error::Release ${TAG} has ${count} asset(s) matching '${pattern}', expected at least ${minimum}."
              missing=true
            else
              echo "OK: ${count} asset(s) match '${pattern}' (minimum ${minimum})."
            fi
          done <<'PATTERNS'
          *.AppImage:2
          *.dmg:4
          *.exe:4
          *.flatpak:2
          *.snap:2
          *.zip:8
          PATTERNS
          if [[ "$missing" == 'true' ]]; then
            echo '::error::Refusing to publish an incomplete release.'
            exit 1
          fi
          echo 'verified=true' >> "$GITHUB_OUTPUT"
      - env:
          GH_TOKEN: '${{ github.token }}'
          TAG: '${{ steps.guard.outputs.tag }}'
        if: "steps.assets.outputs.verified == 'true'"
        name: 'Publish release'
        run: 'gh release edit "$TAG" --draft=false'
      - env:
          GH_TOKEN: '${{ github.token }}'
          REPO: '${{ github.repository }}'
          RUN_ID: '${{ github.run_id }}'
        if: 'always()'
        name: 'Delete prior non-publishing Release runs'
        run: |
          delete_run() {
            local id="$1" reason="$2"
            echo "Deleting prior Release run ${id} (${reason})."
            gh run delete --repo "$REPO" "$id" || true
          }
          published_step_skipped() {
            local id="$1"
            local step_conclusion
            step_conclusion=$(gh api "repos/${REPO}/actions/runs/${id}/jobs" \
              --jq '[.jobs[].steps[] | select(.name == "Publish release")][0].conclusion' 2>/dev/null || echo '')
            [[ "$step_conclusion" == 'skipped' ]]
          }
          gh run list --repo "$REPO" --workflow 'Release' \
            --json databaseId,status,conclusion --limit 100 \
            | jq -r --arg self "$RUN_ID" \
                '.[] | select((.databaseId|tostring) != $self) | select(.status == "completed") | [.databaseId, .conclusion] | @tsv' \
            | while IFS=$'\t' read -r id conclusion; do
                [[ -z "$id" ]] && continue
                case "$conclusion" in
                  skipped|cancelled|neutral)
                    delete_run "$id" "$conclusion"
                    ;;
                  success)
                    if published_step_skipped "$id"; then
                      delete_run "$id" 'success without publish'
                    fi
                    ;;
                esac
              done
      - env:
          GH_TOKEN: '${{ github.token }}'
          REPO: '${{ github.repository }}'
          RUN_ID: '${{ github.run_id }}'
        if: "steps.guard.outputs.proceed != 'true' || steps.check.outputs.ready != 'true'"
        name: 'Cancel this run so it can be cleaned up'
        run: |
          echo 'This run did not publish; cancelling so a later run can remove it.'
          gh run cancel --repo "$REPO" "$RUN_ID" || true
          sleep 30
name: 'Release'
'on':
  workflow_run:
    types:
      - 'completed'
    workflows:
      - 'Build'
      - 'Flatpak'
      - 'Prettier'
      - 'Snap'
      - 'Spelling'
      - 'Tests'
      - 'clang-format'
      - 'markdownlint'
permissions:
  actions: 'write'
  contents: 'write'
